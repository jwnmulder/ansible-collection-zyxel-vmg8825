---
- debug: msg="START common/zyxel_dal_raw.yaml on connection={{ ansible_connection }}"

- name: test zyxel_dal_raw - get/static_dhcp
  zyxel_dal_raw:
    api_oid: static_dhcp
    api_method: get
  register: result

- assert:
    that:
      - "result.changed == false"
      - "result.obj is defined"

- debug: msg="END common/zyxel_dal_raw.yaml on connection={{ ansible_connection }}"
