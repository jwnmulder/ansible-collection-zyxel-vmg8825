---
- debug: msg="START common/zyxel_static_dhcp.yaml on connection={{ ansible_connection }}"

- name: test zyxel_static_dhcp
  zyxel_static_dhcp:
  register: result

- assert:
    that:
      - "result.changed == false"
      - "result.obj is defined"

- debug: msg="END common/zyxel_static_dhcp.yaml on connection={{ ansible_connection }}"
