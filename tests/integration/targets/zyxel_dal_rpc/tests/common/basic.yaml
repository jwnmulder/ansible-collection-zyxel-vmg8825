---
- debug: msg="START common/zyxel_dal_rpc.yaml on connection={{ ansible_connection }}"

- name: test zyxel_dal_rpc
  zyxel_dal_rpc:
    oid: static_dhcp
    method: get
  register: result

- assert:
    that:
      - "result.changed == false"
      - "result.obj is defined"

- debug: msg="END common/zyxel_dal_rpc.yaml on connection={{ ansible_connection }}"
